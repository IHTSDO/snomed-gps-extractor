# opensnomed-extractor

Start by running the class and passing in the file locations & names of the concepts, descriptions and preferences file. i.e.

```bash
java ExtractTerms files/concepts.txt files/descriptions.txt files/prefs.txt
```
